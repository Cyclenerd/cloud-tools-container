name: "Test"

on:
  push:
    paths:
      - 'Dockerfile'
  pull_request:
  workflow_dispatch:

jobs:

  docker:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Build 🧪
        id: docker_build
        run: |
          podman build . \
          --platform linux/amd64,linux/arm64 \
          --tag cloud-tools-container:test